package com.billbharo.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * This file defines the color palette for the Bill Bharo application's light theme.
 * These colors are based on the Material 3 design system.
 */

// Light Theme Colors

/** The primary color of the app, used for main UI elements. */
val Primary = Color(0xFF6750A4)

/** The color for content (text, icons) displayed on top of the primary color. */
val OnPrimary = Color(0xFFFFFFFF)

/** A container color for elements that need less emphasis than the primary color. */
val PrimaryContainer = Color(0xFFEADDFF)

/** The color for content on top of the primary container color. */
val OnPrimaryContainer = Color(0xFF21005D)

/** The secondary color, used for less prominent UI elements. */
val Secondary = Color(0xFF625B71)

/** The color for content on top of the secondary color. */
val OnSecondary = Color(0xFFFFFFFF)

/** A container color for secondary elements. */
val SecondaryContainer = Color(0xFFE8DEF8)

/** The color for content on top of the secondary container color. */
val OnSecondaryContainer = Color(0xFF1D192B)

/** The tertiary color, used for contrasting accents. */
val Tertiary = Color(0xFF7D5260)

/** The color for content on top of the tertiary color. */
val OnTertiary = Color(0xFFFFFFFF)

/** A container color for tertiary elements. */
val TertiaryContainer = Color(0xFFFFD8E4)

/** The color for content on top of the tertiary container color. */
val OnTertiaryContainer = Color(0xFF31111D)

/** The color used to indicate errors. */
val Error = Color(0xFFB3261E)

/** The color for content on top of the error color. */
val OnError = Color(0xFFFFFFFF)

/** A container color for error states. */
val ErrorContainer = Color(0xFFF9DEDC)

/** The color for content on top of the error container color. */
val OnErrorContainer = Color(0xFF410E0B)

/** The background color for the app's screens. */
val Background = Color(0xFFFFFBFE)

/** The color for content on top of the background color. */
val OnBackground = Color(0xFF1C1B1F)

/** The color for surfaces like cards and bottom sheets. */
val Surface = Color(0xFFFFFBFE)

/** The color for content on top of surfaces. */
val OnSurface = Color(0xFF1C1B1F)

// Additional Colors for App

/** A green color used to indicate success states. */
val GreenSuccess = Color(0xFF4CAF50)

/** A red color used to indicate danger or destructive actions. */
val RedDanger = Color(0xFFF44336)

/** An amber color used for warnings or to draw attention. */
val AmberWarning = Color(0xFFFFC107)
